# dsh-manager 开发 Backlog

> 本文件是开发期记录，**不打包进 tarball**（package.json 的 files 白名单不含它）。
> 来源：0.7.0 非 UI 层三轮完整评审（与用户逐项确认后，三轮发现均已修复）。
> 编号沿用评审编号，每条含现状与建议方案；开工前按编号与用户确认优先级。

## ✅ 已修复记录（0.7.0）

- 第一轮 🔴/🟠：dup/损坏可删、lost 拒删/拒启（假 already）、ps1 同文件链守卫、扫描 sha 判定（不重放脚本）、部署根失配按 lost、导入查重全量、script/override 可选 marker 防二次应用、空链过期状态丢弃。
- 第二轮：**完全平等化**（引擎零内置补丁：删除 `SEED_PATCHES` 与补种逻辑；示例补丁降级为 `examples/` 数据文件，随包分发、手动导入；删除不复活）；**C** 目标文件被外部删除后禁用不复活文件；**C2** 多文件部分状态启用报"不一致"而非假 already；**D** 同补丁重复文件条目结构校验拒绝。对应冒烟回归在第 10 节（10i–10l）。
- 第三轮：**恢复出口**（标记在、状态无的补丁禁用 = 刷新空链基线，响应带 `reset` 标志，不再卡死）；**沙箱传参改名** `__dsh_patch_text__`（防脚本 `__text__` 遮蔽）；README 补三条使用须知（改 id 先禁用 / 损坏声明不清理伴随文件 / 恢复出口）。对应冒烟回归：10e 扩展 + 10m。
- 第四轮：**刷新按钮复活**（面板 effect 依赖补 `tick`，操作后立即重载）；**损坏状态/设置文件不再 500**（按无状态/默认值处理，收养自愈）；**RECOVERY.md 写失败不打断主流程**（内层捕获 + 日志）。对应冒烟回归：10n。
- 第五轮：**空类别可见**（扫描 categories 来自真实目录列表，新建空类别即可向它导入）；**资源上限**（导入内容 ≤1MB、script 源码 ≤1MB、override ≤10MB、前置/互斥 ≤50 项）；**同名损坏文件拒绝唯一定位**（提示手动处理）；**ps1 端到端复测**（status/-apply/-restore 独立探针全过）。对应冒烟回归：10o。
- 第六轮（UI 层）：**A** 面板级 Esc 守卫（输入框有内容时 Esc 只取消输入、不关面板）；**B** 重命名取消语义（Esc 取消后 blur 不再提交；`renameSession` ref 防 Enter+blur 双重提交）；**C** 补丁卡显示目标文件路径（scan 响应新增 `targets`，首条 + 计数，title 全量）；**H** 类别增/删/改 notice 改为「已创建/已重命名/已删除类别」，lost 徽标独立错误色（`skm-badge-lost`）；**I** 类别变更后「导入到」自动回落「默认」。D/E/F/G（搜索、折叠、聚合导入反馈、导入预览）入 backlog 23–26。

## 🟡 健壮性 / 债务

### 9. 快照孤儿无 GC
- **现状**：`writeSnapshot` 成功、状态写入失败（罕见磁盘错误）的窗口会留下永不被引用的快照文件；内容寻址去重但不会自动清。
- **建议**：扫描时对比 `.state/files` 引用集合与 `snapshots/` 文件集合，删除无引用且 mtime 超过 24h 的快照；或在 -restore 里顺带全清（已有）。

### 10. ps1 与引擎双实现漂移
- **现状**：同一套"状态/链/校验"逻辑在 `lib/index.js`（主）与 `reapply-deployment-patches.ps1`（CLI 兜底）各实现一份，已经漂移过一次（-apply 覆盖同文件链，已修）。
- **建议**：长期把 ps1 收窄为"状态报告 + -restore 还原"两个职责，-apply 提示用面板；或每次改引擎时强制跑 ps1 功能测试（当前为手工）。

### 11. 导入不携带伴随文件
- **现状**：`+` 按钮/拖放只接受 `.dsh-patch.json` 声明文件；script 的 `.dsh-patch.js`、override 的目录必须手动放进补丁目录。
- **建议**：拖放/选择支持多文件，识别伴随文件（.dsh-patch.js / override 目录）一并入库；结构校验仍以声明文件为准。

### 12. Windows 保留名未预拒
- **现状**：`CATEGORY_RE` 允许 CON/PRN/AUX/NUL/COM1…，mkdir 时以底层错误冒出来（500）。
- **建议**：类别名与补丁文件名校验增加 Windows 保留名黑名单，返回友好错误。

### 13. 扫描与写操作不在同一串行队列
- **现状**：`patchScan` 未进 `serialized` 队列，启用/禁用事务进行中时，6s 轮询可能读到中间态（一闪而过，下次轮询自愈）。
- **建议**：扫描也走串行队列（或对扫描加"事务进行中"标志，读到就等下一轮）。

### 14. 多浏览器同账户一致性
- **现状**：两个页面同时操作时，写入已串行化 ✓，但另一端 UI 状态要等下一轮 6s 轮询才对齐。
- **建议**：可选——host 侧广播状态变更事件（走现有事件总线）让客户端即时刷新；或接受轮询延迟。

### 18. apply 字段仅作者声明
- **现状**：Q11 确认过"含宿主端文件自动升级 restart"，但引擎只是原样回显 manifest 的 `apply`（无法可靠判定宿主端）。
- **建议**：文档化现状，或加启发式（目标路径在 `lib/` 下且非 `client.js` → 建议 restart）。

### 19. override 仅支持文本文件
- **现状**：override 内容以 UTF-8 文本读写，二进制文件会损坏。
- **建议**：README 声明"补丁只适用于文本文件"；远期可加 base64 模式。

### 20. 禁用报错不点名链中失效成员
- **现状**：链成员脚本文件被手动删除后，禁用同文件其它补丁时报"重建链失败 + 脚本文件名"，不含补丁 id。
- **建议**：`replayChain` 错误信息带 `id`。

### 21. RECOVERY.md 复原命令路径为占位符
- **现状**：手册里写 `<scripts 目录>\reapply-deployment-patches.ps1`。
- **建议**：写明实际两个位置（项目 scripts/ 与 ~/.dsh/scripts/）。

### 22. 面板确认态无超时复位
- **现状**：补丁面板的"确认启用可执行补丁？/确认删除？"二次确认没有 4 秒自动复位（技能卡片有），一直挂着直到用户再点。
- **建议**：与 SkillCard 同款——confirming 状态挂 4s setTimeout 自动清除。

### 23. 补丁搜索/过滤（第六轮 UI 评审 D，暂缓）
- **现状**：补丁多了只能靠类别分组逐条看，无关键词/状态过滤。
- **建议**：面板头部加搜索框，按 id/name/description/目标文件路径做包含匹配 + 状态筛选（全部/已启用/未启用/异常）。

### 24. 类别折叠（第六轮 UI 评审 E，暂缓）
- **现状**：类别标题行无折叠交互，多类别时列表很长。
- **建议**：类别标题行加折叠箭头，按类别记住折叠态（本会话内存即可）。

### 25. 多文件导入只显示最后结果（第六轮 UI 评审 F，暂缓）
- **现状**：`importFiles` 串行导入 N 个文件，notice 被逐个覆盖，失败也混在一起。
- **建议**：聚合反馈「导入 3 个：成功 2（a、b），失败 1（c：原因）」+ 错误区并列展示。

### 26. 导入前预览（第六轮 UI 评审 G，暂缓）
- **现状**：选择文件即直接写入补丁目录，无确认步。
- **建议**：先展示清单（name/id/类别/目标文件/是否脚本）再点确认导入；拖放同路径。

## 远期（暂不做，记录在案）

### 15. script 沙箱硬隔离
- **现状**：`node:vm` + 纯函数（无 IO 全局）+ 3s 超时 + 输出上限 + 「允许可执行补丁」总闸。vm 非硬安全边界，已文档化接受的权衡。
- **远期**：若需硬隔离，改 `worker_threads` + `resourceLimits`（内存/CPU 上限），引擎侧 IO 不变。

### 16. 同文件链式堆叠（Q8 预留设计）
- **现状**：同文件多补丁靠"链重放 + 干跑裁定"已支持（replace/script 可共存，override 排他）。
- **远期**：更复杂的"补丁相对前序状态编写"场景（官方更新后整链重放合并）不在 v1 范围；优先引导用户把同文件多补丁合并为一个多 files 补丁。

### 17. 操作反馈细化（面板）
- **现状**：lost 状态禁用时，主机把整个文件链重置为当前内容（官方更新的正确行为），但响应未区分"普通禁用"与"重置刷新"。
- **建议**：`patchDisable` 返回 `reset: true` 时，面板显示"官方已更新：已把当前文件刷新为新基线，请重新启用需要的补丁"。

## 🚀 服务面板（规划中）v2 待办

> 来源：2026-08-19 服务面板设计 grilling。用户逐轮确认；已定决策如下，v2 暂缓项见下。
> 已定：Q1=b 启停+运行状态；Q2=独立面板「本地服务」+ DSH 侧边栏新按钮（本地仓库面板不管理服务）；Q3=b 治理层 `_governance/services.json` 集中配置（面板写、agent 生成模板）；Q4=a 面板直接 spawn；Q5=a 服务随 dsh 停止 + PID 文件对账；Q6=b 每项目命名服务列表 {name,cwd,command,args,env,port?}；Q7=b PID+端口探活；Q8=a 总开关+确认弹窗；Q9=仓库面板自动分类（有第三方 origin→GitHub项目；无 origin 或自己 origin→本地项目；删除 rootTypes 手动切换）；Q10=启动失败仅卡片错误+notice（不自动重启）；Q11=不加例外（反代项目 4 个 git 仓库归 GitHub项目）；Q12=注册入口在仓库卡片右上角「注册服务/已注册」角标，注册后才出现在本地服务面板；Q14=点注册只建空条目（不填配置），角标「已注册」点击跳本地服务面板；Q15=本地服务面板内可「移除注册」（二次确认，先自动停止该项目运行中服务）；Q16=裸目录检测用文件规则（含 `.git`/`.dsh` 或顶层有非隐藏、非 README* 普通文件→项目卡；否则视为分类目录继续递归）。待定：多进程启动顺序（v2 候选）。

- **v2-Q1c**：运行日志浏览、健康检查、失败自动重启（v1 只做启动/停止 + 运行状态）。
- **v2-Q5b**：服务脱离 dsh 独立存活、跨重启自动恢复（v1 服务随 dsh 停止 + PID 文件对账）。
- **待议**：多进程服务的依赖/启动顺序（sub2api 后端+前端、grok-register Python+Node），v1 先手动逐个启动，排序进 v2 候选。
